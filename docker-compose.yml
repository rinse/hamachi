version: "3.7"

services:
  hamachi:
    image: hamachi
    build: .
    container_name: hamachi
    network_mode: host
    privileged: true
#   environment:
#     ACCOUNT:
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./config:/var/lib/logmein-hamachi:rw
    command:
      - bash
      - startup.sh
